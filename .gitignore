.DS_Store
Thumbs.db
desktop.ini
*.log

.vscode/
.idea/
*.swp
*.swo
*~

node_modules/
build/
.cache/

# dist/ is intentionally tracked — it ships shareable builds (BreakFree.html,
# break-free-hosted.zip, SHARE.md) so the repo is a self-contained way to
# distribute the app to friends.

.env
.env.local

__pycache__/
*.pyc
.venv/
venv/

# Source PDFs and the extracted .txt are intentionally tracked.
# knowledge.json is intentionally tracked so the app loads without rebuilding.
